## 2024-06-04 - Custom Switch Controls Missing ARIA Labels
**Learning:** Custom toggle switch components (e.g. `<button role="switch">`) frequently lack `aria-label`s in this app.
**Action:** Always verify that custom switch elements have explicit `aria-label`s or `aria-labelledby` attributes for screen reader compatibility.
